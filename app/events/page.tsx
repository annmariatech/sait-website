'use client';import {useState} from 'react';import {PageHero} from '@/components/page-hero';import {EventList} from '@/components/event-list';import {events,pastEvents} from '@/data/site';
export default function Events(){const [year,setYear]=useState('All');const [cat,setCat]=useState('All');const years=['All','2026','2025','2024'];const cats=['All','Tech','Career','Community','Events'];const past=pastEvents.filter(x=>(year==='All'||x[0]===year)&&(cat==='All'||x[1]===cat));return <main><PageHero kicker="Events / 03" title="A calendar that actually moves." description="Technical sessions, competitions, career prep, community gatherings and the occasional very late night in a lab."/><div className="container-grid"><section className="rule-section"><div className="eyebrow mb-5">Flagship programme</div><div className="grid md:grid-cols-2 gap-px bg-line border border-line">{events.filter(e=>e.flagship).map(e=><article key={e.id} id={e.id} className="bg-paper p-7 min-h-64"><div className="mono text-[9px] text-navy">{e.date} · {e.time} · {e.venue}</div><h2 className="display text-4xl mt-10 leading-none">{e.title}</h2><p className="text-sm leading-6 text-neutral-600 mt-4 max-w-xl">{e.description}</p><button className="btn btn-navy mt-6">Register ↗</button></article>)}</div></section><section className="rule-section"><div className="eyebrow mb-4">All upcoming</div><EventList/></section><section className="rule-section pb-24"><div className="flex flex-wrap gap-2 mb-7">{years.map(y=><button key={y} onClick={()=>setYear(y)} className={`mono text-[9px] uppercase px-3 py-2 border ${year===y?'border-navy bg-navy text-white':'border-line'}`}>{y}</button>)}{cats.map(c=><button key={c} onClick={()=>setCat(c)} className={`mono text-[9px] uppercase px-3 py-2 border ${cat===c?'border-navy text-navy':'border-line'}`}>{c}</button>)}</div><div className="border-t border-line">{past.map((p,i)=><div key={i} className="grid grid-cols-12 gap-3 border-b border-line py-4 text-sm"><div className="col-span-2 mono text-[10px]">{p[0]}</div><div className="col-span-2 mono text-[9px] uppercase text-navy">{p[1]}</div><div className="col-span-6">{p[2]}</div><div className="col-span-2 text-right mono text-[9px] text-neutral-500">{p[3]}</div></div>)}</div></section></div></main>}
