'use client';import {useState} from 'react';import {people} from '@/data/site';
export function PeopleGrid(){const [team,setTeam]=useState('All');const teams=['All','Executive','Tech','Media','Events','PR','Content'];const list=team==='All'?people:people.filter(p=>p.team===team);return <><div className="flex flex-wrap gap-2 mb-7">{teams.map(t=><button key={t} onClick={()=>setTeam(t)} className={`mono text-[9px] uppercase tracking-widest px-3 py-2 border ${team===t?'border-navy bg-navy text-white':'border-line'}`}>{t}</button>)}</div><div className="grid sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-px bg-line border border-line">{list.map(p=><article key={p.name} className="bg-paper p-5 min-h-64"><div className="aspect-[4/3] bg-[#e8e5de] border border-line flex items-end justify-between p-3"><span className="display text-4xl text-neutral-500">{p.name.split(' ').map(x=>x[0]).join('')}</span><span className="mono text-[9px] uppercase">{p.year}</span></div><p className="mono text-[9px] uppercase text-navy mt-4">{p.team} · {p.role}</p><h3 className="display text-xl mt-1">{p.name}</h3><p className="text-sm mt-2 text-neutral-600 leading-5">{p.bio}</p></article>)}</div></>}
