'use client';
import Link from 'next/link';import {Menu,X,ArrowUpRight} from 'lucide-react';import {useState} from 'react';import {nav} from '@/data/site';
export function SiteHeader(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-line bg-paper/95 backdrop-blur-sm"><div className="container-grid"><div className="flex h-16 items-center justify-between"><Link href="/" className="display text-xl font-bold tracking-tight">SAIT<span className="text-navy">.</span></Link><nav className="hidden lg:flex items-center gap-6">{nav.slice(0,6).map(([l,h])=><Link key={h} href={h} className="mono text-[10px] uppercase tracking-[.12em] link-underline">{l}</Link>)}<Link href="/notifications" className="mono text-[10px] uppercase tracking-[.12em] text-navy">Updates</Link></nav><div className="flex items-center gap-3"><Link href="/logger" className="hidden md:inline-flex btn btn-navy">Activity Logger</Link><button aria-label="Open menu" onClick={()=>setOpen(true)} className="lg:hidden p-2"><Menu size={20}/></button></div></div></div>{open&&<div className="fixed inset-0 bg-paper z-[60] lg:hidden"><div className="container-grid"><div className="flex h-16 items-center justify-between border-b border-line"><span className="display text-xl font-bold">SAIT<span className="text-navy">.</span></span><button onClick={()=>setOpen(false)} aria-label="Close menu"><X size={24}/></button></div><nav className="py-8">{nav.map(([l,h],i)=><Link onClick={()=>setOpen(false)} key={h} href={h} className="flex items-center justify-between border-b border-line py-5 display text-3xl"><span>{String(i+1).padStart(2,'0')}</span><span>{l}</span><ArrowUpRight size={22}/></Link>)}</nav></div></div>}</header>}
