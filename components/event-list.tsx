import Link from 'next/link';import {ArrowUpRight} from 'lucide-react';import {events} from '@/data/site';
export function EventList({limit}:{limit?:number}){return <div>{events.slice(0,limit||events.length).map((e,i)=><article key={e.id} className="grid grid-cols-12 gap-3 border-t border-line py-5 hover:bg-black/[.02] transition"><div className="col-span-2 md:col-span-1 mono text-[10px] text-neutral-500">{String(i+1).padStart(2,'0')}</div><div className="col-span-10 md:col-span-3"><div className="mono text-[10px] text-navy">{e.date} · {e.time}</div><div className="mono mt-1 text-[9px] uppercase text-neutral-500">{e.venue}</div></div><div className="col-span-12 md:col-span-6"><h3 className="display text-xl md:text-2xl font-medium">{e.title}</h3><p className="mt-2 text-sm leading-6 text-neutral-600">{e.description}</p></div><div className="col-span-12 md:col-span-2 flex md:justify-end items-start gap-3"><span className="mono text-[9px] uppercase border border-line px-2 py-1">{e.type}</span><Link href={`/events#${e.id}`} aria-label={`Open ${e.title}`} className="text-navy"><ArrowUpRight size={18}/></Link></div></article>)}</div>}
